import 'package:flutter/material.dart';

class Closet extends StatelessWidget {
  Widget build(BuildContext context) => new Center(
          child: Container(
        child: Text('This is the closet'),
      ));
}
