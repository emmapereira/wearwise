import 'package:flutter/material.dart';

class Tracker extends StatelessWidget {
  Widget build(BuildContext context) => new Center(
          child: Container(
        child: Text('This is the tracker'),
      ));
}
